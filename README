# A python photo booth

A simple photo booth written in python with a GUI created in Qt.

## Download
This project includes a photo library via submodule. To clone both at once, use

    git clone --recursive git@github.com:Nepomuk/photobooth.git

## Requirements
Additional requirements can be easily installed via pip

    pip install -r requirements.txt

## Compile
For the first execution, compile the graphical interface with

    ./compileAndRun.sh
